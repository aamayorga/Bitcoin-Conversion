# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BitcoinConversion' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BitcoinConversion
  pod 'Alamofire'

  target 'BitcoinConversionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BitcoinConversionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
